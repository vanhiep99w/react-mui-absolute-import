import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    app: {
      main: "#c8dadf",
      dark: "#92b0b3",
      light: "#39bfd3"
    }
  }
});

export default theme;
