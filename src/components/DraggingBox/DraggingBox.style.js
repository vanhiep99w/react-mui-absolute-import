import { Box, styled } from "@mui/material";

export const Container = styled(Box)({
  maxWidth: 600,
  width: "fit-content",
  margin: "0 auto",
  display: "flex",
  direction: "row",
  alignItems: "strech",
  gap: 10,
  justifyContent: "center",
  borderRadius: 5,
  padding: 10
});
